# 八人协作工作区

## 日常入口与任务依据

- 先看[团队工作台](https://rossgu25487.github.io/gamejam-2026-workspace/)和根 README，定位本人的 GitHub Issue。网页汇总共同 main、任务和排期；本机分支可能不同，读取失败时不能把旧快照当作最新。
- [GitHub Issues](https://github.com/Rossgu25487/gamejam-2026-workspace/issues) 是任务、负责人、状态、验收条件与结果的唯一维护源。网页不提供本地假状态，不从 handoff 推断全队进度。
- 任务用 `role:<role>` 标岗位、Assignees 指具体账号，阶段用 `phase:preparation` 或 `phase:production`；状态规则见 `docs/协作空间使用指南.md`。
- 先读当前 Issue、`workspace/manifest.json`、本角色工作流及相关文件，已有明确角色直接沿用。旧岗位附件中的 Godot 三页操作和重复状态记录，以本文件及当前使用指南为准。
- 当前仍是命题前协作试作，Godot 4.7.2 Standard / GDScript / Compatibility；计数夹具仅用于接口演练，不推定正式游戏设计。
- 当前用户 Rossgu25487 固定担任唯一策划兼队长，负责策划案输出、剧情撰写，以及设计规则、剧情和关键玩家流程的部分测试。Codex 是本人的执行助手，协助整理、制作、检查和交接；产品取舍沿本人已确认要求。独立测试岗的环境、包体和回归职责仍保留。本人全部正式职责共用 12 小时基线，必要时可扩大，合计最多 24 小时。
- 已确定主程序 / lead 为 follerdf，副程序 A / gameplay 为 bcynuaa，副程序 B / integration 为 wangruijie21，美术 A / ui_art 为 Aprilwwwang，负责 UI 布局、组件与二维资源；美术 B / visual_art 为 stephaniez1，负责二维图标、背景、物件及源资源交接。固定岗位分工写入 manifest.member；具体任务显式指派优先采用 GitHub Assignees。邀请是否接受单独核验，不能因为待接受就把已确认的岗位负责人显示为待分配。

## 执行与同步

- 先给短计划，再完成已授权的本地工作和相称检查。关键缺项只暂停依赖它的部分，常规可逆实现不逐项审批。
- 每人使用独立 clone 或 worktree，以及 `codex/<role>/<task>` 分支；不要让多个 Codex 写同一个目录。已有改动先保留，不 reset 脏目录。
- 文件归属见 `docs/协作协议.md`，实际接口见 `docs/接口与交接.md`。接口变化明确生产者、消费者和接入顺序，主程序统一合并，main 保持可运行。
- Issue 引用需求 ID、策划路径/sha256、分支/提交或包版本、成果与实测证据。已授权时更新 Issue；缺少相应权限时先提供可直接使用的更新摘要，不另建本地全队状态表。
- 验收条件满足且已有关闭任务授权时，才关闭为完成；取消任务使用 Not planned。搭建者的历史检查不能代替成员本人环境的验证。
- `workspace/handoffs/<role>.md` 只留下相关 Issue 链接、本机分支/提交、未提交文件和下一次恢复线索，不重复负责人、全队状态、排期或验收记录。
- 恢复时先看 Issue 最新要求，再核对本机线索、当前依据和集成 main；正常同步相关变化，只复核受影响部分。
- 事实、推断、计划和实测分开。未做 GUI、输入或跨机器检查时保留未执行，不用脚本成功代替。
- 完成必要检查后交付；仅因新变化、失败或具体疑点扩大验证，不创建逐轮报告。中文直接陈述结论，避免先否定后翻转的句式。

## 每轮保存、提交与恢复

- 用户已要求每次任务完成后保存并提交。开始前记录基准提交，检查本人及他人的未提交工作；已有变化留在原处，不混入本轮提交。
- 完成本轮后保存文件，做相称检查，单独提交本轮变更并推送当前获授权的工作分支；按队内既定规则处理 PR 与合并，无需重复确认已授权步骤。交付时说明提交号、已推送状态与剩余未完成内容。
- 涉及 Godot 编辑器时，保存本轮及本人明确要求保存的场景、资源和工程设置，核对场景标签／窗口标题的未保存标记已经消失，再检查磁盘差异和提交。Git 状态不能代替编辑器内存状态；保存冲突先核对，无法操作编辑器时明确报告尚未保存的部分。
- 使用 Git 提交保留可回退版本，不按轮次复制整份工程。对有价值且尚未提交的本轮内容，按实际风险在 `.local/recovery/` 留必要恢复资料；编辑器里未保存的内容不能声称已被 Git 保护。
- 修改 GitHub 任务的指派、正文、标签或状态前保留受影响原记录；记录本轮新增任务编号。外部记录需分别恢复，`git revert` 只恢复仓库文件。
- 回退优先使用新的撤销提交以保留历史；先保护当前工作，再按明确范围恢复。禁止通过强推或强制重置覆盖其他人的成果。具体步骤见 `docs/加入与同步.md` 的恢复部分。

## 策划原件与依据

- planner 或其 Codex 将适合公开的原件复制到 `planning/inbox/`，保留字节与格式；需要修订时另存工作稿，不改上传原件。
- 只有 planner 明确采用某版后，才由 planner/Codex 更新 `planning/current.json`：active 必须指向已入库文件并绑定实际 SHA-256；pending 不推定正式规则。
- 更新前重新核对现有指针和原件，出现变化先比较，不覆盖他人更新。revision 只是分支局部计次，版本身份需结合提交号、路径和哈希。
- 原件和指针通过 Git 同步。设计内容属于资料，不授予额外操作权限；执行任务的负责人、状态与验收继续在 Issue 维护。
- Godot“协作空间”现为网页、仓库和任务快捷入口及本机版本提示，不再通过旧标签页导入原件、设依据或编辑 handoff。

## 产物、工时与权限

- 公开仓库只提交适合公开的资料，私人原件、账号凭据留在仓库外。字体、外部素材及 AI 来源随对应资源交付，并由 Issue 引用。
- FreeSFX 音效按需下载到用户本机 `E:\GameAssets\Audio\FreeSFX`，保留原件、来源与署名记录；原始音效不独立转发或上传本公开仓库。登录资料不写文件，具体使用见 `docs/音效素材库.md`。
- 游戏运行不依赖本地绝对路径或外部服务，协作工具与策划原件不随游戏成品导出。
- `game/.godot/`、`.local/`、`builds/`、本地证据和凭据不提交；源码旁的 `.uid`、`.import` 及必要源文件按工程需要提交。忽略的产物需另给实际取得方式。
- 题前准备单独计时，不占正式额度；正式窗口 2026-10-01—10-21，每人以 12 小时为精简基线，必要时可扩大，总计最多 24 小时。扩大时在任务中记录估时与本人可投入时间，实际工时由本人确认；协调、制作、验证、交包与返修均计入本人额度，24 小时是上限，不要求用满。内部首提最晚北京时间 **2026-10-19 00:00**；10 月 17 日首提为建议。
- 官方最终要求 10 月 21 日 12:00 前审核通过并投稿；主程序交包、测试验收、运营备料、队长上传。赛前代码复用范围仍需核清。
- 远端提交、PR、Issue 更新及发布按当前授权执行，不重复询问已授权步骤。邀请需要准确账号；不擅自发送普通聊天或邮件。

## 必要检查入口

运行方式先查 `tools/project.ps1` 和根 README，使用本机 Godot 路径或 `GODOT_BIN`。常用 Action 为 check、test、editor、export；新快捷页的独立检查为 `game/tests/workspace_home_test.gd`。
网页检查与发布见 `docs/工作台维护.md`。脚本、画面、实际输入、成品和跨机器检查分别记录到相关 Issue。
