# 工作台冒烟测试

2026-09-24，本地检查基线 `2e7dfda`。本次修复提交可从本文件的 Git 历史定位，未发布到线上。

结论：本地核心操作通过，发现的 4 处问题已修复并回归。自动提醒的真实发送、定时运行和成员收信仍未验证，不能据此认定通知全链路已开通。

## 检查结果

| 范围 | 结果 | 已核对内容 |
|---|---|---|
| 规则与构建 | 61 项通过 | 负责人优先级、进度分母、截止解析、发送窗口、防重复、发送前复核、构建失败保留快照 |
| 浏览器操作 | 15 组通过 | 八岗与六种状态筛选、搜索、中文输入法、视图分享、岗位记忆、成员分组、分页、空列表、提醒草稿、异常恢复 |
| 批量提醒 | 模拟通过 | 标准模板与编辑草稿分开说明；超过 20 项分组；中途失败保留部分结果，停止后续发送 |
| 视觉与尺寸 | 通过 | 1440、768、390、320 像素宽度无横向溢出；已查看桌面、手机截图及提醒弹窗 |
| 真实只读数据 | 通过 | 12:23 北京时间从 GitHub 刷新成功；共同 main 为 `3d04cd7`，15 项任务；策划准备进度 2/3，当前待处理 1 项 |
| 资料与发布清单 | 通过 | 显示提交中的 4 个资料入口、8 份岗位文件存在；发布清单包含全部浏览器模块；生成数据未纳入提交 |
| 本地服务 | 通过 | 只提供 site 文件；仓库外路径返回 404，写入请求返回 405 |

浏览器自动冒烟使用固定任务场景，GitHub API 与外链均被测试响应替代，避免成员真实进度变化影响回归结论。另有独立真实只读会话核对当前 GitHub 数据；该会话没有脚本异常或远端写请求。

## 已修复问题

1. **岗位记忆关闭后重新勾选。** 复现：取消勾选后从首页重新进入。修复后保存关闭偏好，后续岗位选择不会被偷偷记住。
2. **主数据刷新失败时提醒状态不更新。** 复现：先显示工作流可用，再让主数据和工作流请求失败。修复后两项独立核验，较早返回的旧请求也不能覆盖新结果。
3. **批量中途失败缺少恢复线索。** 复现：模拟前项成功、当前项发送超时。修复后保留成功评论链接，当前项明确标为待核实，后续项标为未尝试；命令非零退出，不重试 POST。
4. **网页批量范围超过脚本上限。** 复现：一个成员有 25 项未完成任务。修复后分为 20 项和 5 项两组，复制的编号完整且不重复。

## 尚未进行的外部验证

- 未上传本轮代码、发布网页或启用 GitHub Actions 提醒。
- 未向真实任务发表评论，未触发真实定时任务，未确认任何成员收到通知或邮件。
- 当前 11 项未完成任务均没有独立截止时间，其中测试、运营任务还缺少确定负责人。自动提醒会跳过这些任务。
- 未进行另一台电脑及 Firefox、Safari 的兼容性测试。本次浏览器验证使用 Windows 上的 Chromium 内核，手机部分为窄屏模拟。

## 复现与证据

运行方法见 [工作台维护](../docs/工作台维护.md)。浏览器脚本为 [smoke-dashboard.mjs](../tools/smoke-dashboard.mjs)，结果和截图保存在当前工作目录忽略的 `.local/dashboard-review/`：

- `smoke-results.json`：15 组模拟场景结果。
- `smoke-live-desktop.png`、`smoke-live-mobile.png`：真实只读数据页面。
- `smoke-mobile-fixture.png`：固定测试场景的窄屏页面，不代表真实项目任务或试玩包。

上述证据仅在本机保存；它们不随公开仓库自动分发。回退时先保护后续工作，再对本次修复提交执行 `git revert`，保留历史。
