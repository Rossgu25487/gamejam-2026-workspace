# Local editor state, credentials, generated builds and QA captures
.local/
game/.godot/
game/.godot/**
game/.mono/
builds/
qa/evidence/local/
*.tmp
*.tmp-*
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.translation
# Dashboard data is rebuilt from GitHub; never commit local snapshots.
/site/data/snapshot.json
/site/data/github-data.mjs
# Verified local extraction of the already tracked v2 distribution archive.
/分发包/八人岗位工作流_v2/
# Source .uid and resource-side .import files ARE tracked.
