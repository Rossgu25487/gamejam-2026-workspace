# 美术 A · UI 源资源

当前按钮、面板、颜色与系统字体统一由 `../../ui/theme.tres` 提供，使用 Godot 原生主题资源，可在 Inspector 修改。

以后确需额外组件底图时，将可编辑源文件和必要导出放在本目录。界面文字与布局继续由副程序 B 的原生控件承载，图标与背景由美术 B 在 `../visual/` 维护。

交接写入仓库根目录 `workspace/handoffs/ui_art.md`；调整字段前与界面接入者对齐，避免各自修改同一个主题文件。
